
<h1><%= @stock_family.generic_family.name %></h1>
	
<%= form_for(@stock_family) do |f| %>
  <% if @stock_family.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stock_family.errors.count, "error") %> prohibited this brand from being saved:</h2>

      <ul>
      <% @stock_family.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Subir imagen" %><br>
    <%= f.file_field :photo %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

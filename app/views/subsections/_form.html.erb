<%= form_for([@section,@subsection]) do |f| %>
  <% if @subsection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subsection.errors.count, "error") %> prohibited this subsection from being saved:</h2>

      <ul>
      <% @section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h2>Almacén: <%= @section.warehouse.branch %></h2>

  <h3>Seccion: <%= @section.code %></h3>

  <div class="field">
    <%= f.label :code, "Código" %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :nombre, "Nombre" %><br>
    <%= f.text_field :name %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "Guardar subsección", class:"btn btn-info" %>
  </div>
  <br>
<% end %>

<%= form_for([@section,@subsection]) do |f| %>
  <% if @subsection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subsection.errors.count, "error") %> prohibited this subsection from being saved:</h2>

      <ul>
      <% @section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h2>Seccion: <%= @section.code %></h2>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :name %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "Crear Subsection", class:"btn btn-info" %>
  </div>
  <br>
<% end %>
